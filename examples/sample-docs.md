# Sample docs

See the project [README](../README.md) for how to run the link checker.

To make CI fail on purpose, add a local link to a file that does not exist,
for example a path like `./does-not-exist.md` (do not add that here so CI stays green).
